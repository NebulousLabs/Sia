language: go

go:
  - 1.5.1

install:
  - make dependencies
  - test -z "$(go fmt ./...)"
  - test -z "$(golint -min_confidence=1.0 ./modules/host)"
  - make

script: make test && make test-long && make cover && make bench

sudo: false
